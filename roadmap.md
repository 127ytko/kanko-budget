# 今後の開発ロードマップ

## フェーズ1: スクレイピングエンジンの構築（データ収集の自動化）
現在の静的なCSV読み込みから、実際にウェブサイトを巡回してデータを収集する仕組みを構築する。

### 1-1. 汎用クローラーの実装
- **入力**: `target_urls.csv` （ID, 自治体名, URL）
- **処理**:
    - 各URL（記事一覧ページ等を想定）にアクセス。
    - ページ内のリンクから条件（PDF, 「予算」などのキーワード）に合致するものを抽出。
    - 必要に応じて詳細ページへ遷移し、最終的なドキュメントURLを取得。
- **出力**: 抽出された生データ（日付, タイトル, URL, 関連テキスト）。

### 1-2. キーワードフィルタリングとデータ整形
- これまでハードコードまたは別管理していたキーワードを `tags.csv` から動的に読み込む。
- 抽出したテキストから「観光」「インバウンド」等のキーワードが含まれる記事のみをフィルタリングして保存対象とする。
- 日付情報をテキスト（例：「2024年4月1日更新」）から `YYYY-MM-DD` 形式に正規化する。

### 1-3. データ保存と重複排除
- **保存先**: `multi_municipality_budget_analysis.csv`
- **ロジック**:
    - URL（PDFのリンク先）をキーとして重複チェック。
    - 既存データにある場合はスキップ、または日付が新しい場合のみ更新。
    - 新規データのみを追記するモードの実装。

## フェーズ2: アプリケーション統合（UIからの制御）

### 2-1. 実行トリガーの実装
- 設定ページ（`/settings`）に「データ更新（スクレイピング実行）」ボタンを配置。
- ボタン押下時に非同期でスクレイピング処理を実行（ユーザーを待たせない工夫）。

### 2-2. 実行ステータスの可視化
- 実行中、完了、エラーなどの状態を表示。
- 設定ページのリストにある「状態」バッジを、実際のスクレイピング結果（成功/失敗）連動させる。

## フェーズ3: 分析精度の向上（AI活用）

### 3-1. LLMによる要約とタグ付け
- 単なるキーワードマッチングだけでなく、抽出したテキスト（またはPDFの中身）をLLMに渡し、要約 (`Excerpt_Summary`) を生成。
- 「この予算が具体的に何に使われるか」を抽出。

### 3-2. PDF解析
- PDFファイルをダウンロードし、テキスト化して解析するフローの追加（現在はリンク元のテキスト依存）。

## デプロイ情報
- **Vercel Project**: kanko-budget
- **Production URL**: https://kanko-budget.vercel.app/
